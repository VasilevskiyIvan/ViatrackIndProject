<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.motion.widget.MotionLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/motion_tracker_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layoutDescription="@xml/scene_tracker"
    android:background="@color/colorWhite">

    <org.osmdroid.views.MapView
        android:id="@+id/mapView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/tracker_panel"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <include layout="@layout/layout_tracker_content"
        android:id="@+id/tracker_panel"
        android:layout_width="match_parent"
        android:layout_height="280dp"
        app:layout_constraintBottom_toBottomOf="parent" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_start_event"
        android:layout_width="wrap_content"
        android:layout_height="56dp"
        android:text="Начать событие"
        app:icon="@drawable/play"
        android:backgroundTint="@color/colorAccent"
        android:textColor="@color/colorWhite"
        app:cornerRadius="28dp"
        android:elevation="50dp"
        android:translationZ="50dp"
        android:stateListAnimator="@null"
        app:layout_constraintBottom_toTopOf="@id/tracker_panel"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginStart="16dp"
        android:layout_marginBottom="20dp"
        android:visibility="gone"
        tools:visibility="visible" />

    <ImageButton
        android:id="@+id/btn_expand_panel"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:src="@drawable/arrow_up"
        android:background="@null"
        android:contentDescription="Expand"
        android:padding="12dp"
        android:scaleType="centerInside"
        app:layout_constraintBottom_toTopOf="@id/tracker_panel"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_margin="16dp"
        android:elevation="4dp" />

</androidx.constraintlayout.motion.widget.MotionLayout>